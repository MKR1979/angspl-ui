#---------------------------------------------Error and Solution ---------------------------------#

1. Error :- "404-Bad Input request"
Solution :- "Method / Columns or fields name must be match with Type, Graphql, APi , PSQL."

2. Error :- "400: Cannot query with Method Name."
Solution :- "New module needs to be added in the "index.js" of Type and Resolver "

3. Error :- "List is not populating any data"
solution :- "1. Where clause of function count and Page/List must be Identical."
             2. Your schema and resolver variable must be identical/ in the api
             3. Same variable must be match at client side while reading data

3. Error :- "Pgcatelog Btrim"
solution :- "Whem you apply trim on any fields which are not being passed from client/api(i.e. undefined)"

4. (a) Error :- "Reading trim issue"
solution :- "Trim cannot be applied on Integer/Number/undefined/null."

4. (b) Error :- "Undefined Reading trim"
Solution :- "All the attributes/ fields with trim must be passed from resolver/Api"

4. (c) Error :- "Unknown Parameters"
solution :- Among (UI's, GraphQl, APi, PSQL)
        (1) Number of columns must be matched.
        (2) Type of columns must be matched.
        (3) Sequence of columns must be matched.

5. Error :- API not calling in code-project view and when we provide hardcoaded value of id api calling starts, then solves backend 
            errors and then restore the code to get dynamic values of id, API calling starts without any changes in the actual code.
   Solution :- 


6. Error :- If we have opened more than 5 files in PSQl, server gets hang/slow/dowm.
   Solution :- Close existing opened tabs/files.


7. Error :- Api not calling, 
   Solution :- 1. if we are passing null value to non-nullable variables, and If you getting null value to non-nulable(ids) variable from api.
               2. if token is diffrent (Create server ApolloClient)
               3. Ensure that your method name should be identical in graphQL (both UI side and API side) and resolver.
               4. no. of parameter and type of parameter shoulb also be identical in GraphQlQL (both UI and API).
               5. Column's type mismatch in API or in SQL, Also ensure that parameter's ORDER should match.
            
8. Error :- on edit click process gets completed but the edited data not updated .
   Solution :- 

9. Error Message :- "context is not defined (this error encounters in addPayment method)".
   solution :- 

10. Error :- Sometimes we got Internal Server Error in network response because of both client side and server side schema.
   solution :- we need to ensure that the structure/variables of both client side schema(graphQl) and server side schema should be identical.

11. Error :- In user lookup if we add "All User" at the top with id =-1, and in our lookup first item is not displaying "All Users" by default.
   solution :- we need to use this "id" in dto as -{ user_id: number | null;   //   user_id: null,}.

12. Error :- CORS error in network response. || error code 429 in network response.


13. Issue :- Date handeling according to IST time or UTC time. 
    Solution :- Date handeling should be done in insert/update like in add attendance or add referral.




Your Location: Lat 23.19251 | Lng 77.34225

Your Location Office:Latitude: 23.192222
                     Longitude: 77.342222

our new Location :
23°11'32.7"N 77°20'32.3"E
23.192402, 77.342314



23°11'32"N 77°20'32"E552 m
 dps:-
 Lat: 23.201387 | Lng: 77.344285